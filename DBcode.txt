CREATE TABLE CAR
(
   CAR_ID IDENTITY NOT NULL PRIMARY KEY,
   BRAND VARCHAR(20) NOT NULL,
   MODEL VARCHAR(20) NOT NULL,
   HORSE_POWER INT NOT NULL,
   ENGINE_CAPACITY  INT NOT NULL,
   PRODUCTION_DATE INT NOT NULL,
   CATEGORY VARCHAR(20) NOT NULL
);
CREATE TABLE PERSON
(
PERSON_ID IDENTITY NOT NULL PRIMARY KEY,
EMAIL VARCHAR(50) NOT NULL,
FIRST_NAME VARCHAR(30) NOT NULL,
LAST_NAME VARCHAR(30) NOT NULL,
PHONE INT NOT NULL 
);
CREATE TABLE RENT
(
RENT_ID IDENTITY NOT NULL PRIMARY KEY,
RETURN_DATE DATE NOT NULL
RENT_DATE DATE NOT NULL,
CAR_ID  INT IDENTITY NOT NULL ,
 FOREIGN KEY (CAR_ID) REFERENCES CAR(CAR_ID),
PERSON_ID  INT IDENTITY NOT NULL,
 FOREIGN KEY (PERSON_ID) REFERENCES PERSON(PERSON_ID)
);


SELECT  P.PHONE,  C.BRAND FROM PERSON P JOIN RENT R ON P.PERSON_ID = R.PERSON_ID JOIN CAR C ON R.CAR_ID = C.CAR_ID WHERE PHONE = phone AND BRAND = brand;



SELECT P.FIRST_NAME, R.RENT_DATE FROM  PERSON P JOIN RENT R ON P.PERSON_ID = R.PERSON_ID JOIN CAR C ON R.CAR_ID = C.CAR_ID where FIRST_NAME = ? and RENT_DATE = ?;